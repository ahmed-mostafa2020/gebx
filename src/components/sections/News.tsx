import SectionTemplate from "../common/reusable/SectionTemplate";
import news from "@assets/news.png";

const News = () => {
  return (
    <SectionTemplate
      image={news}
      title="News & Insights"
      backgroundColor="#0DBDAD4f"
    >
      slider
    </SectionTemplate>
  );
};

export default News;
