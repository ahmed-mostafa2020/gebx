// This file only serves as a placeholder for the root layout
// The actual layout is in [locale]/layout.js

export const metadata = {
  title: "GebX | Home",
  description: "GebX for technology solutions",
};

// This layout is only used for the root redirect
export default function RootLayout({ children }) {
  return children;
}
